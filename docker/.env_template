# SMTP Master Environment Configuration
# Copy this file to .env and update the values

# Database settings
MASTER_DB_ROOT_PASSWORD=change_this_root_password
MASTER_DB_USER=master_admin
MASTER_DB_PASSWORD=change_this_password

# Sync settings
SYNC_INTERVAL=6h  # How often to sync from slaves (used in cron)

# Web interface settings (if enabled)
WEB_PORT=8080